import { ShortendUnitsPipe } from './shortend-units.pipe';

describe('ShortendUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortendUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
