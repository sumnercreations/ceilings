import { StyledSelectDirective } from './styled-select.directive';

describe('StyledSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledSelectDirective();
    expect(directive).toBeTruthy();
  });
});
